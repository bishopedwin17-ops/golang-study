// Write a Go program to find the length of a string text = "Go is great!".
// Required Input:

// A predefined string.

// Expected Output:

// 12

package refrepo

import "fmt"

func Strlength() {
	// Find the length of the string and print it
	text := "this is the string to count its characters"
	fmt.Println(len(text))
}
