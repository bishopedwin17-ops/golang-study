# Module 07: Methods & Interfaces
## shapes
Define an interface `Shape` with `Area() float32`.
Define `Circle` and `Rectangle` structs implementing this interface.
Write `TotalArea(shapes []Shape) float32`.
