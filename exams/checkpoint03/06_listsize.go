// =============================================================
// EXERCISE: listsize
//
// QUESTION: Write a function that returns the number of elements 
//           in a linked list.
//
// EDGE CASES:
//           - If the list is empty (l.Head == nil), return 0.
// =============================================================

package piscine

// ListSize returns the number of elements in l.
func ListSize(l *List) int {
	// ---------------------------------------------------------
	// TODO: 
	// 1. Start a counter at 0.
	// 2. Create a temporary pointer `current` pointing to `l.Head`.
	// 3. Loop: while `current != nil`, increment counter, and 
	//    move `current = current.Next`.
	// 4. Return the counter.
	// ---------------------------------------------------------
	return 0
}
